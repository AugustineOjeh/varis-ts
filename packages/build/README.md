# @usevaris/build

`@usevaris/build` reads the `define` calls in a TypeScript project that uses
`@usevaris/sdk` and writes them to `varis.json`. The Varis CLI runs it on
demand when you run `varis build`. You don't need to install it.
